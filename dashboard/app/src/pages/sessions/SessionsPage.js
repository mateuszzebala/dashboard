import React from 'react'
import { MainTemplate } from '../../templates/MainTemplate'
import { APPS } from '../../apps/apps'

export const SessionsPage = () => {
    return <MainTemplate app={APPS.sessions}></MainTemplate>
}
