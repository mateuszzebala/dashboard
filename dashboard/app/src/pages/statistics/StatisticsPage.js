import React from 'react'
import { MainTemplate } from '../../templates/MainTemplate'
import { APPS } from '../../apps/apps'

export const StatisticsPage = () => {
    return <MainTemplate app={APPS.statistics}></MainTemplate>
}
