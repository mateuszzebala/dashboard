const primary = '#181915'

const darkColors = {
    background: primary,
    font: '#ffffff',
}

export const theme = {
    leftbar: darkColors,
    topbar: {
        background: '#ffffff',
        font: primary,
    },
    button: darkColors,
    input: {
        border: primary,
        font: primary,
        background: '#ffffff',
    },
    checkbox: primary,
    switch: {
        on: primary,
        off: 'grey',
        dot: '#ffffff',
    },
    radio: primary,
    modal: {
        background: '#ffffff',
        font: primary,
    },
    scrollbar: {
        thumb: 'white',
        track: primary,
        width: '5px',
    },
    loading: primary,
}
